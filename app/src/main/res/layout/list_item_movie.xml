<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView android:id="@+id/list_item_movie_title"
        tools:text="Sample Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="8dp"
        android:textStyle="bold"
        android:layout_centerVertical="true" />

    <TextView android:id="@+id/list_item_movie_release_date"
        tools:text="(19XX)"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/list_item_movie_title"
        android:textStyle="italic"
        android:layout_centerVertical="true"/>

    <Button android:id="@+id/list_item_movie_remove_button"
        android:layout_height="wrap_content"
        android:layout_width="48dp"
        android:layout_alignParentRight="true"
        android:text="@string/list_item_movie_remove"
        />

</RelativeLayout>