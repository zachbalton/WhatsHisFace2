<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent" >

    <TextView android:id="@+id/no_results_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        tools:text="No Common Actors Found"
        android:textSize="20sp"
        android:layout_margin="16dp"
        android:text="@string/no_results_found_desc"/>

    <Button android:id="@+id/new_search_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        tools:text="New Search"
        android:text="@string/no_result_search_again_button"
        android:layout_below="@id/no_results_text_view"
        android:layout_centerHorizontal="true"
        android:layout_margin="16dp"
        android:padding="20dp"/>

</RelativeLayout>